jQuery(document).ready(function($) {

"use strict"

  var navMenu = require( "./nav-menu.js" );
  var stickyNav = require( "./sticky-nav.js" );
  var onePageNav = require( "./one-page-nav.js" );
  var siteLoader = require( "./site-loader.js" );
  var jScrollPane = require( "./jscrollpane.js" );

  /*
  ** WOW animations
  */
  new WOW({
    mobile: false
  }).init();

});
